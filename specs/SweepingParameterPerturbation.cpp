#include "SweepingParameterPerturbation.h"

SweepingParameterPerturbation::SweepingParameterPerturbation(QString name, double delta_percentage, int iterations)
    : name(name), delta_percentage(delta_percentage), iterations(iterations)
{
    // Do nothing else for now
}
